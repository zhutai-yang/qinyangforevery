spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:127.0.0.1}:${DB_PORT:3306}/${DB_NAME:tt_event_test}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

  flyway:
    enabled: true
    clean-disabled: true

tt:
  jwt:
    secret: test-jwt-secret-key-for-ci-only-min-32-chars!!
  sync:
    scheduler:
      enabled: false
  files:
    base-dir: ${java.io.tmpdir}/tt-uploads-test
