# 01 - 官网赛事赛程成绩（S08）

## Public API（示例）

- `GET /api/public/events`
- `GET /api/public/events/:id`
- `GET /api/public/events/:id/matches`
- `GET /api/public/matches/:id`
- `GET /api/public/events/:id/preview-matches`

## 实现要点

只读、分页；可选短缓存；不暴露内部字段。

## 视图

见 [d-官网前端视图](../d-官网前端视图/)。
