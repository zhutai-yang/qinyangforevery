# 01 - 数据源配置（S13）

## 表

`ext_data_source`、`ext_sync_log`。

## API

- `CRUD /api/admin/ext-sources`
- `GET /api/admin/ext-sync-logs`
- `POST /api/admin/ext-sources/:id/sync`（`tt.sync.mode`: `direct` | `queue`，见 [02-抓取与调度](./02-抓取与调度.md)）
