# implementation-baseline.md 模板（TT 前端）

> 路径：`乒乓球赛事管理系统（TT）整体规划/d-前端视图设计（前端）/切片UI-N-名称/`

```markdown
# 实现基线：切片UI-N-名称
> **应用**: admin（Element UI）| portal（Vant）
> **后端依赖**: 切片X implementation-baseline.md

## §一 页面清单
| 页面 | 路由 | 组件路径 |

## §二 工程配置
- axios：`client/shared/request.js` 或各端 `api.js`
- baseURL / proxy 约定

## §三 API 清单
| API | 方法 | 路径（相对 baseURL） | 消费页面 |

## §四 复用组件
| 组件 | 路径 |

## §五 owned 范围
- `client/admin/views/...` 或 `client/portal/views/...`
- 对应 `main.js` 路由段
```
